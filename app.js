/**** this is file is belong to 
 * introduction file of JavaScript ***/

// Data types typeof( ) -> in the terminal
typeof (37) === 'number';


typeof ({}) === 'object';

typeof ('hi there') === 'string';
